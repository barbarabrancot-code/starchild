# Registro de Alterações — 04/09/2026 09:24

Escopo: só as mudanças que afetam `onboarding.html`, a pedido da UX.

## Resumo
`onboarding.html` passou a rodar a Variante B em vez da A, e a abertura da conversa guiada para o tópico "Too much on my plate" ganhou uma frase escrita à mão em vez da frase genérica montada por template.

## Alterações por Variante/Módulo

### B/Entry
- Arquivos modificados: `src/onboarding-main.tsx`
- O que mudou: o bootstrap de `onboarding.html` trocou de `./prototype/ConductorApp` para `./prototype-b/ConductorApp` — a página passou a carregar a Variante B (sem a área de Automations) em vez da A.

### B/Onboarding
- Arquivos modificados: `src/prototype-b/onboarding/FirstMeeting.tsx`
- O que mudou: o tópico "Too much on my plate" ganhou um campo `opening` próprio ("Got it. Let's start there. What's on your plate this week that you want me to help handle?"), lido antes do template genérico em `openingFor`. Os outros quatro tópicos (Work, Something I'm building, A decision, Something personal) continuam usando a frase montada por template, sem alteração.

## Arquivos Adicionados
- (nenhum)

## Arquivos Removidos
- (nenhum)
