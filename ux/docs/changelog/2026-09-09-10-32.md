# Registro de Alterações — 09/09/2026 10:32

Escopo: novo entry point `onboarding-b.html` (arquitetura "Version B" — Jobs em vez de Automations).

## Resumo
Novo link publicado que roda o mesmo onboarding, mas com a área hoje chamada "Automations"
renomeada pra "Jobs" (e os ícones de Jobs/Agents trocados entre si) — só nesse entry point,
sem afetar `app.html` nem o `onboarding.html` original.

## Alterações por Variante/Módulo

### A/Onboarding B
- Arquivos adicionados: `onboarding-b.html`, `src/onboarding-b-main.tsx`
- Arquivos modificados: `vite.config.ts`, `src/prototype/ProductSidebar.tsx`, `src/prototype/ChatScreen.tsx`
- O que mudou:
  - `onboarding-b.html` carrega a mesma árvore `prototype/` do `app.html` (onde a feature de Jobs/Automations já existe), só que via um entry point próprio.
  - Em `ProductSidebar.tsx`, o item de navegação lê a URL (`onboarding-b`) pra decidir o rótulo: "Jobs" nesse entry point, "Automations" em todo o resto — e troca também os ícones dos itens Jobs/Agents entre si só nesse caso.
  - Em `ChatScreen.tsx`, a resposta introdutória de automations ("Automations are the things I keep handling...") usa o mesmo rótulo, pra não ficar "Jobs" na barra lateral e "Automations" no texto.

## Arquivos Adicionados
- `onboarding-b.html`
- `src/onboarding-b-main.tsx`

## Arquivos Removidos
- (nenhum)
