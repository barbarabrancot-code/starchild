# Registro de Alterações — 04/09/2026 11:50

Escopo: só as mudanças que afetam `onboarding.html`.

## Resumo
Criação de agente agora entra direto no roster, resposta do Starchild no chat principal ganhou balão, e a barra lateral ganhou "Quests" com Connectors virando placeholder.

## Alterações por Variante/Módulo

### B/Agents
- Arquivos modificados: `src/prototype-b/agents/AgentsWorkspace.tsx`
- O que mudou: criar um agente agora abre uma conversa em branco com "New agent" como nome clicável (a mesma porta que abre a gaveta em qualquer agente existente), com o campo de mensagem já funcional. O agente é adicionado ao roster imediatamente, então a gaveta lateral já mostra "AGENT" (não mais "NEW AGENT") e os botões "Pause agent"/"Delete agent" embaixo de Colour, no lugar do antigo botão "Create agent" — o fluxo de rascunho separado foi removido.

### B/Chat
- Arquivos modificados: `src/prototype-b/ChatScreen.tsx`, `src/prototype-b/StepFlow.tsx`, `src/prototype-b/ProductSidebar.tsx`
- O que mudou: a resposta do Starchild no chat principal agora aparece dentro de um balão (fundo, cantos arredondados), e a linha de "processando" ("I'll take a look and work through this for you.") some assim que a resposta em balão chega, em vez de continuar na tela como uma segunda fala solta. Na barra lateral, um item "Quests" (troféu com indicador vermelho) foi adicionado logo abaixo de Connectors, acima de "Search Chats"; o botão de Connectors virou um placeholder — continua visível, mas não navega para lugar nenhum por enquanto.

## Arquivos Adicionados
- (nenhum)

## Arquivos Removidos
- (nenhum)
