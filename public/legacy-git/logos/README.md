Coloque aqui os SVGs das marcas usados no grid de integrações, um arquivo por nome referenciado em `TILES` (ex: `notion.svg`, `github.svg`, `gitlab.svg`, `vercel.svg`, `codex.svg`, `cursor.svg`, `railway.svg`, `chatgpt.svg`, `sentry.svg`).

Enquanto o arquivo não existir, o tile cai num fallback com as duas primeiras letras do nome.
