# Design

- `landing.html` — entrada Vite da landing (`/design/landing.html` no servidor local).
- `design-system.html` — referência visual e de componentes.
- `assets/` — imagens, vetores, vídeos e fontes compartilhados pelas duas páginas.

Execute `npm run dev` a partir da raiz do projeto para trabalhar nas páginas.
